export type Credentials = {
    email: string;
    password: string;
};

export type AccessToken = {
    accessToken: string;
    user: any;
};

export type User = {
    fullName: string;
    email: string;
    password: string;
}